Hi <%= @user.name %>,
Welcome to <%= ENV['APP_NAME'] %>! Click on the link below to activate your account:
<%= generate_url("#{ENV["ORIGIN"]}/activate", :activation => @user.activation_token, :email => @user.email) %>